target
**/target

*.bin
*.elf
*.hex
*.uf2

.DS_Store
__pycache__/

# Note: k9-pad-firmware/Cargo.lock is intentionally committed (binary
# project needs reproducible builds). The workspace root Cargo.lock is
# managed separately by the workspace.
