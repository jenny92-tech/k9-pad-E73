# 独立二进制

> 调试/测试用独立固件目标

## 地位

开发辅助，不参与正式固件构建。

## 逻辑

各 bin 独立运行，用于隔离测试特定硬件功能。

## 约束

- 每个 bin 必须有 `#![no_std]` `#![no_main]`

## 业务域清单

| 名称 | 文件 | 职责 |
|------|------|------|
| USB 测试 | `usb_test.rs` | USB CDC ACM 功能验证 |
